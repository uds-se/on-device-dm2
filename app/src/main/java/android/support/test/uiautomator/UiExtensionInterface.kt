package android.support.test.uiautomator

/*
fun AccessibilityService.longClick(x: Int, y: Int, timeout: Long) =
	//performGlobalAction()

fun AccessibilityService.click(x: Int, y: Int, timeout: Long) =
	interactionController.clickAndSync(x,y,timeout)
//	interactionController.clickNoSync(x,y)
*/